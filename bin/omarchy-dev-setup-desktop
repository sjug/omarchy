#!/bin/bash

# omarchy:summary=Compatibility wrapper for omarchy overlay setup
# omarchy:hidden=true

set -euo pipefail

script_dir=$(cd -- "$(dirname -- "$(realpath "${BASH_SOURCE[0]}")")" && pwd)
exec "$script_dir/omarchy-overlay-setup" "$@"
